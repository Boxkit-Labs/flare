{
  "name": "flare-backend",
  "version": "1.0.0",
  "type": "module",
  "description": "",
  "main": "dist/server.js",
  "scripts": {
    "dev": "npx tsx --watch src/server.ts",
    "build": "tsc && node -e \"const fs = require('fs'); const path = require('path'); const destDir = path.join('dist', 'db'); if (!fs.existsSync(destDir)) fs.mkdirSync(destDir, { recursive: true }); fs.copyFileSync(path.join('src', 'db', 'schema.sql'), path.join(destDir, 'schema.sql'))\"",
    "start": "node dist/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@stellar/mpp": "^0.4.0",
    "@stellar/stellar-sdk": "^14.6.1",
    "@x402/core": "^2.8.0",
    "@x402/express": "^2.8.0",
    "@x402/stellar": "^2.8.0",
    "cors": "*",
    "dotenv": "*",
    "express": "^5.2.1",
    "firebase-admin": "*",
    "mppx": "^0.5.9",
    "node-cron": "*",
    "pg": "^8.20.0",
    "uuid": "*",
    "ws": "^8.20.0",
    "x402-stellar": "*"
  },
  "devDependencies": {
    "@types/cors": "*",
    "@types/express": "*",
    "@types/node": "*",
    "@types/pg": "^8.20.0",
    "@types/uuid": "*",
    "@types/ws": "^8.18.1",
    "nodemon": "*",
    "ts-node": "*",
    "typescript": "*"
  }
}
